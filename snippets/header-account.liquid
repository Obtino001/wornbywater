{%- if shop.customer_accounts_enabled and section.settings.enable_account_btn -%}
  {% if section.settings.enable_account_modal_component %}
     {%- if is_mobile == true -%}
      {%- if customer -%}
        <div class="menu-drawer__utility-account">
          <div class="menu-drawer__utility-account-header">
            <div>
              <shopify-account
                menu="{{ section.settings.customer_account_menu | default: "customer-account-main-menu" }}"
                style="display: flex; align-items: center;"
              >
                <div slot="signed-out-avatar">
                  <div class="header__icon header__icon--account link--text header__icon--summary focus-inset menu-drawer__utility-name" role="button">
                    {{ customer.name }}
                  </div>
                </div>
              </shopify-account>
            </div>
            <a class="menu-drawer__utility-logout" href="{{ routes.account_logout_url }}">{{ 'customer.log_out' | t }}</a>
          </div>
          <ul class="menu-drawer__utility-account-body">
            <li class="menu-drawer__utility-account-cart">
              <a href="{{ routes.cart_url }}">{{ 'customer.account.shopping_cart' | t }}</a>
              {%- if cart != empty -%}
                <div class="cart-count-bubble">
                  {%- if cart.item_count < 100 -%}
                    <span aria-hidden="true">{{ cart.item_count }}</span>
                  {%- endif -%}
                  <span class="visually-hidden">{{ 'sections.header.cart_count' | t: count: cart.item_count }}</span>
                </div>
              {%- endif -%}
            </li>
            <li class="menu-drawer__utility-account-link">
              <a href="{{ routes.account_profile_url }}">
                {{ 'customer.account.title_1' | t }}
                {{ 'customer.account.title_2' | t }}
              </a>
            </li>
          </ul>
        </div>
      {%- else -%}
        <shopify-account
          menu="{{ section.settings.customer_account_menu | default: "customer-account-main-menu" }}"
          style="display: flex; align-items: center;"
        >
          <div slot="signed-out-avatar">
            <div class="link--text header__icon--summary focus-inset button menu-drawer__account link focus-inset" role="button" style="min-width: calc(100vw - 3rem);">
              {% render 'icon-account' %}
              {%- liquid
                if customer
                  echo 'customer.account_fallback' | t
                else
                  echo 'customer.log_in' | t
                endif -%}
            </div>
          </div>
        </shopify-account>
      {%- endif -%}
    {%- else -%}
      <div class="header__account {% if is_mobile %}header_component__account{% endif %}">
        <shopify-account
          menu="{{ section.settings.customer_account_menu | default: "customer-account-main-menu" }}"
          style="display: flex; align-items: center;"
        >
          <div slot="signed-out-avatar">
            <div class="header__icon header__icon--account link--text header__icon--summary focus-inset" role="button">
              {% render 'icon-account' %}
            </div>
          </div>
        </shopify-account>
      </div>
    {% endif %}
  {%- elsif is_mobile == true -%}
    {%- if customer -%}
      <div class="menu-drawer__utility-account">
        <div class="menu-drawer__utility-account-header">
          <span class="menu-drawer__utility-name">{{ customer.name }}</span>
          <a class="menu-drawer__utility-logout" href="{{ routes.account_logout_url }}">{{ 'customer.log_out' | t }}</a>
        </div>
        <ul class="menu-drawer__utility-account-body">
          <li class="menu-drawer__utility-account-cart">
            <a href="{{ routes.cart_url }}">{{ 'customer.account.shopping_cart' | t }}</a>
            {%- if cart != empty -%}
              <div class="cart-count-bubble">
                {%- if cart.item_count < 100 -%}
                  <span aria-hidden="true">{{ cart.item_count }}</span>
                {%- endif -%}
                <span class="visually-hidden">{{ 'sections.header.cart_count' | t: count: cart.item_count }}</span>
              </div>
            {%- endif -%}
          </li>
          <li class="menu-drawer__utility-account-link">
            <a href="{{ routes.account_profile_url }}">
              {{ 'customer.account.title_1' | t }}
              {{ 'customer.account.title_2' | t }}
            </a>
          </li>
        </ul>
      </div>
    {%- else -%}
      <a href="{%- if customer -%}{{ routes.account_profile_url }}{%- else -%}{{ routes.account_login_url }}{%- endif -%}" class="button  menu-drawer__account link focus-inset">
        {% render 'icon-account' %}
        {%- liquid
          if customer
            echo 'customer.account_fallback' | t
          else
            echo 'customer.log_in' | t
          endif -%}
      </a>
    {%- endif -%}
  {%- else -%}
    <details-disclosure class="header__account">
      <details>
        <summary class="header__icon header__icon--account link--text  header__icon--summary focus-inset modal__toggle" aria-haspopup="dialog" role="button">
          {% render 'icon-account' %}
        </summary>
        <div class="header__account-modal modal__toggle color-background-2" role="dialog" aria-modal="true">
          {%- if customer -%}
            <div class="header__account__account">
              <div class="header__account__name">{{ customer.name }}</div>
              <ul class="header__account__body">
                <li class="header__account__link">
                  <a href="{{ routes.account_profile_url }}">
                    {{ 'customer.account.title' | t }}
                  </a>
                </li>
              </ul>
              <a class="header__account__logout button " href="{{ routes.account_logout_url }}">{{ 'customer.log_out' | t }}</a>
            </div>

          {%- else -%}
            <a href="{%- if customer -%}{{ routes.account_profile_url }}{%- else -%}{{ routes.account_login_url }}{%- endif -%}" class="button  header__account__login link  focus-inset">
              {% render 'icon-account' %}
              {%- liquid
                if customer
                  echo 'customer.account_fallback' | t
                else
                  echo 'customer.log_in' | t
                endif -%}
            </a>
          {%- endif -%}
        </div>
      </details>
    </details-disclosure>
  {%- endif -%}
{%- endif -%}
